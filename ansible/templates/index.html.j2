<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Status do Ambiente | {{ cloud_provider | upper }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .gradient-bg {
            /* Gradiente dinâmico: Azul para Azure, Laranja/Roxo para AWS */
            background: {{ 'linear-gradient(135deg, #0078d4, #00182b)' if cloud_provider == 'azure' else 'linear-gradient(135deg, #ff9900, #3b0764)' }};
        }
        .glass-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
    </style>
</head>
<body class="gradient-bg text-white min-h-screen flex items-center justify-center p-4">

    <div class="w-full max-w-2xl glass-card rounded-2xl shadow-2xl p-8 md:p-12 text-center transform hover:scale-105 transition-transform duration-500">
        
        <header class="mb-8">
            <div class="inline-block {{ 'bg-blue-600' if cloud_provider == 'azure' else 'bg-orange-500' }} text-white text-sm font-bold px-6 py-2 rounded-full mb-4 shadow-lg">
                {{ cloud_provider | upper }} - AMBIENTE {{ environment_name | upper }}
            </div>
            <h1 class="text-4xl md:text-5xl font-bold tracking-tight">Framework de Automação IaC</h1>
            <p class="text-blue-100 mt-2">Provisionamento e Configuração Multicloud</p>
        </header>

        <main class="text-left bg-black/20 p-6 rounded-lg border border-white/20">
            <h2 class="text-xl font-bold mb-4 border-b border-white/20 pb-2">Detalhes do Servidor</h2>
            <div class="space-y-3 text-blue-500">
                <div class="flex justify-between">
                    <span class="font-semibold text-white">Provedor Cloud:</span>
                    <span class="font-bold text-white">{{ cloud_provider | upper }}</span>
                </div>
                <div class="flex justify-between border-t border-white/5 pt-2">
                    <span class="font-semibold text-white">Hostname:</span>
                    <span class="text-blue-100">{{ ansible_hostname }}</span>
                </div>
                <div class="flex justify-between border-t border-white/5 pt-2">
                    <span class="font-semibold text-white">IP Privado:</span>
                    <span class="text-blue-100">{{ ansible_default_ipv4.address }}</span>
                </div>
                <div class="flex justify-between border-t border-white/5 pt-2">
                    <span class="font-semibold text-white">Status do Deploy:</span>
                    <span class="text-green-400 font-bold">● ONLINE</span>
                </div>
                 <div class="flex justify-between border-t border-white/5 pt-2">
                    <span class="font-semibold text-white">URL Oficial:</span>
                    <a href="https://{{ environment_name }}-{{ cloud_provider }}.alissonlima.dev.br"
                       class="text-blue-300 hover:text-white hover:underline transition-colors">
                       {{ environment_name }}-{{ cloud_provider }}.alissonlima.dev.br
                    </a>
                </div>
            </div>
        </main>

        <footer class="mt-8">
            <p class="text-xs text-blue-200">Projeto de TCC - Alisson Lima - 2026</p>
        </footer>

    </div>

    <script>
        // Script para exibir o timestamp do deploy
        console.log("Servidor {{ ansible_hostname }} carregado em: " + new Date().toISOString());
    </script>

</body>
</html>